package com.kelvaya.ecobee.server

import com.kelvaya.ecobee.test._

class ScheduleSpec extends ZioServerTestSpec {

  "A Kelvaya schedule" must "have a name" in (pending)
  it must "store a full Ecobee schedule" in (pending)
  it must "be renameable" in (pending)
  it must "be storable" in (pending)
  it must "be able to be applied as the live schedule" in (pending)

  "Schedules" must "be queryable" in (pending)
  they must "be deletable" in (pending)
  they must "be creatable from an existing schedule" in (pending)
  they must "be editable without being the live schedule" in (pending)
  they must "be creatable from the live schedule" in (pending)

}
